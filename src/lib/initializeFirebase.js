
// This is a placeholder file to fix the build error
// The Firebase initialization has been replaced with our backend services

export const initializeFirebaseData = () => {
  console.log('Firebase initialization has been replaced with backend services');
};

export default initializeFirebaseData;
